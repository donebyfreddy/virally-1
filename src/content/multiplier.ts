export const multiplier = {
  id: "multiplier",
  eyebrow: "The Multiplier",
  headline: "One brief. As many shots as you decide.",
  body: "Move the controls and watch the campaign resize. Every figure below is calculated from your settings — not a stored example — so you can see exactly what a given amount of ambition costs to produce.",
  calculationNote:
    "Scripts = concepts × hooks. Assets = scripts × languages × formats. Posts = assets. All figures computed live from the controls.",
  aggregationNote:
    "Assets are grouped per concept above 24 so the diagram stays readable. The totals remain exact.",
} as const;
